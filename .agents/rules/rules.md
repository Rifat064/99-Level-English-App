---
trigger: always_on
---

Before running each build prompt or 'go to the next phase prompt', check the build plan readme file if not checked before.
after each successful run, ``always update the project state readme file``.